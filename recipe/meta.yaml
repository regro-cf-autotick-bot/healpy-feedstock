{% set name = "healpy" %}
{% set version = "1.12.6" %}
{% set sha256 = "b4b5ce174c5662e34e12ba387d0c1c498b51a99c562f20966d0b2dd39c218650" %}

package:
    name: {{ name|lower }}
    version: {{version}}

source:
    url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: {{ sha256 }}
    patches:
      - macos-c++-linking.patch  # [osx]

build:
    number: 1000
    script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"
    skip: True  # [win]

requirements:
    build:
        - pkg-config
        - {{ compiler('fortran') }}
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
    host:
        - cython
        - python
        - setuptools
        - numpy
        - pip
        - pytest-runner
        - six
    run:
        - python
        - matplotlib
        - {{ pin_compatible('numpy') }}
        - six
        - astropy
        - scipy
        - pytest-runner

test:
    imports:
        - healpy
        - healpy.test
    requires:
        - pytest
        - nose

about:
    home: http://github.com/healpy
    license: GPL-2.0
    summary: Healpix tools package for Python

extra:
    recipe-maintainers:
        - lpsinger
        - mwcraig
        - pkgw
        - zonca
