{% set name = "healpy" %}
{% set version = "1.12.7" %}
{% set sha256 = "17bdf657ee94c70c91308ddac7bcc327bd96f66610f622e7e03e5635167a6538" %}

package:
    name: {{ name|lower }}
    version: {{version}}

source:
    url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: {{ sha256 }}
    patches:
      - macos-c++-linking.patch  # [osx]

build:
    number: 1000
    skip: True  # [win]

requirements:
    build:
        - pkg-config
        - {{ compiler('fortran') }}
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
    host:
        - cython
        - python
        - setuptools
        - numpy
        - pip
        - pytest-runner
        - six
    run:
        - python
        - matplotlib
        - {{ pin_compatible('numpy') }}
        - six
        - astropy
        - scipy
        - pytest-runner

test:
    imports:
        - healpy
        - healpy.test
    requires:
        - pytest
        - nose

about:
    home: http://github.com/healpy
    license: GPL-2.0
    summary: Healpix tools package for Python

extra:
    recipe-maintainers:
        - lpsinger
        - mwcraig
        - pkgw
        - zonca
