{% set name = "healpy" %}
{% set version = "1.20.0" %}
{% set sha256 = "03b0e1551ae235c7290e9bfdf69fa9cdc194f8a2be51198c1924b15d826102ed" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - macos-c++-linking.patch    # [osx]
    - cross-rpath-flag-workaround.patch  # [aarch64]
    - win-setup-pkgconfig.patch  # [win]

build:
  number: 0
  skip: true  # [py<310]

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - cython                                 # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - pkg-config
    - openssl
    - libopenssl-static  # [unix]
    - libtool  # [unix]
    - make  # [unix]
    - autotools_clang_conda  # [win]
    - compiler-rt  # [win]
    - llvm-openmp  # [win]
    - {{ compiler("fortran") }}
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
  host:
    - cfitsio
    - cython
    - python
    - setuptools
    - setuptools-scm
    - numpy
    - pip
    - zlib
    - llvm-openmp  # [win]
  run:
    - python
    - matplotlib-base
    - astropy-base
    - scipy
    - llvm-openmp  # [win]

test:
  source_files:
    - test
    - setup.py
  imports:
    - healpy
  requires:
    - cython
    - pytest
    - pytest-cython
    - pytest-doctestplus
    - requests
  commands:
    - pytest --doctest-plus --doctest-cython --pyargs healpy
    # Use ./test (not bare "test") to avoid setup.py "test" argv guard while
    # remaining shell-agnostic (Windows cmd does not expand globs like test/*.py).
    - pytest ./test

about:
  home: http://github.com/healpy
  license: GPL-2.0-only
  license_file: COPYING
  summary: Healpix tools package for Python
  doc_url: http://healpy.readthedocs.org/
  dev_url: http://github.com/healpy/healpy

extra:
  recipe-maintainers:
    - lpsinger
    - mwcraig
    - pkgw
    - zonca
